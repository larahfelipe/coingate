import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  Wrapper: {
    display: 'flex',
    justifyContent: 'center',
    borderRadius: '10rem',
    padding: '0.15rem 0',
    boxShadow: '0 0 0 1px rgba(0, 255, 0, 0.5)'
  }
}));
