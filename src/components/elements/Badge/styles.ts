import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  Wrapper: {
    display: 'flex',
    justifyContent: 'center',
    borderRadius: '10rem',
    padding: '0.15rem 0'
  }
}));
